package runtime

import (
	"testing"
)

func TestContainer(t *testing.T) {
	// id := "device123"
	// container := NewContainer()
	// stateMarchine, err := statem.NewState(context.Background(), nil, &statem.Base{ID: id}, nil)

	// assert.Equal(t, nil, err)
	// container.Add(stateMarchine)
	// assert.Equal(t, stateMarchine, container.Get(id))
	// container.Remove(id)
}
